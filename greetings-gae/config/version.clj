(ns version)

(def config
  {:app-id "hello-gae"  ;; (clojure.string/replace +project+ #"/" ".")
   :version "0-1-0-snapshot"})
;; (clojure.string/lower-case (clojure.string/replace +version+ #"\." "-"))})
